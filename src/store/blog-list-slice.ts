import { createSlice } from "@reduxjs/toolkit";

const initialState={}

const blogListSlice = createSlice({
    name:'blogList',
    initialState,
    reducers:{
        
    }
});

export const {} = blogListSlice.actions;
export default blogListSlice.reducer;
